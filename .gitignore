# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage
coverage.xml
htmlcov/

# MS Word lock/temp files (created when the report .docx is open)
~$*.docx
~WRL*.tmp
.ipynb_checkpoints/

# Virtual environments
.venv/
venv/
env/

# MLflow local tracking store
mlruns/
mlartifacts/
mlflow.db

# Data — the raw download is reproducible via src/data/download.py, so ignore it.
# The CLEANED dataset (data/processed/) IS committed as an assignment deliverable.
data/raw/

# The trained model + metadata ARE committed so the Docker image and graders
# can run inference without retraining. (Remove models/ from git if you prefer
# to regenerate it — training is fully reproducible with random_state=42.)

# Generated report draft (regenerate with scripts/build_report_docx.py).
# Commit YOUR final report (with screenshots) under a different name instead.
reports/Heart_Disease_MLOps_Report_DRAFT.docx

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log
