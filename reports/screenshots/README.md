# Screenshots

Place your own screenshots here for the report (the academic-integrity note requires original artefacts from *your* runs). Suggested captures:

| File | What to capture |
|------|-----------------|
| `mlflow_runs.png` | MLflow UI runs table comparing the 3 models |
| `mlflow_run_detail.png` | One run's metrics + logged ROC/confusion-matrix artifacts |
| `github_actions.png` | A green CI run showing lint → test → train → docker-build |
| `docker_build.png` | Successful `docker build` output |
| `docker_run_predict.png` | `/predict` response via curl or Swagger UI |
| `kubectl_pods.png` | `kubectl get pods,svc` with pods Running |
| `k8s_predict.png` | `/predict` call through the LoadBalancer service |
| `prometheus_targets.png` | Prometheus targets page showing the API as UP |
| `grafana_dashboard.png` | Grafana dashboard with live request/prediction metrics |
| `swagger_ui.png` | The FastAPI `/docs` page |
