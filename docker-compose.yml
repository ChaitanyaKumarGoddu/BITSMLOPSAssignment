# Task 8 — local monitoring stack: API + Prometheus + Grafana.
# Bring the whole observability setup up with:  docker compose up --build
services:
  api:
    build: .
    image: heart-disease-api:latest
    container_name: heart-api
    ports:
      # Host 8001 -> container 8000, so this runs alongside the Kubernetes
      # deployment (which uses host 8000). Prometheus scrapes the container on
      # the Docker network at api:8000, unaffected by this host mapping.
      - "8001:8000"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:8000/health').status==200 else 1)"]
      interval: 30s
      timeout: 5s
      retries: 3

  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    depends_on:
      - api

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
    depends_on:
      - prometheus
